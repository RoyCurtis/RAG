{
  "name": "rag",
  "description": "Generates National Rail-style announcements",
  "repository": "https://github.com/RoyCurtis/RAG",
  "license": "MIT",
  "version": "0.0.1",
  "dependencies": {},
  "devDependencies": {
    "http-server": "^0.11.1",
    "npm-run-all": "^4.1.2",
    "stylus": "^0.54.5",
    "typescript": "^2.8.3"
  },
  "scripts": {
    "serve": "http-server -s -c-1 -r",
    "watch-tsc": "tsc -w",
    "watch-tsc-vox": "tsc -w -p vox/",
    "watch-stylus": "stylus -m -w css/index.styl",
    "watch": "npm-run-all --parallel watch-tsc watch-tsc-vox watch-stylus serve"
  }
}
